{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "license": "MIT",
  "devDependencies": {
    "turbo": "^1.5.6"
  }
}
